# Implementation Plan: CLI Foundation

**Branch**: `001-cli-foundation` | **Date**: 2026-03-15 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-cli-foundation/spec.md`

## Summary

Build the minimal interactive CLI for Muggi-Sensei: a terminal-based
REPL that uses a swappable provider, maintains in-memory
conversation history, and supports an extensible slash-command system
with `/help` and `/exit` as the initial built-in commands. Plain text
input and output only.

## Technical Context

**Language/Version**: TypeScript on Node.js 20+ (ES modules)
**Primary Dependencies**: Node.js built-ins for CLI flow; local `codex exec` for backend responses
**Storage**: In-memory only (no persistence in this feature)
**Testing**: Vitest
**Target Platform**: Linux, macOS, Windows (any Node.js environment)
**Project Type**: CLI application
**Performance Goals**: < 3 seconds for non-LLM operations (startup,
command execution); LLM latency is API-dependent
**Constraints**: < 50 MB idle memory; single runtime dependency
**Scale/Scope**: Single user, single session, local terminal

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1
design.*

| Principle | Status | Evidence |
|---|---|---|
| I. Simplicity First (YAGNI) | PASS | 1 runtime dependency. No frameworks for 2 commands. readline built-in. Plain text output. No features beyond spec. |
| II. CLI-First Interface | PASS | Terminal is the only interface. stdin/stdout/stderr protocol. No GUI or IDE integration. |
| III. Modular Architecture | PASS | Clear module boundaries: session management, command registry, LLM client, input loop. Each independently testable. |
| IV. Pedagogical Integrity | PASS | This feature is infrastructure — it does not implement teaching logic. The Muggi Loop will be layered on top via system prompt and future features. No shortcuts introduced. |
| V. Test-After Discipline | PASS | Tests will be written after implementation, covering module contracts (command registry, LLM client) and critical user journeys (session loop). |

No violations. Complexity Tracking section not needed.

## Project Structure

### Documentation (this feature)

```text
specs/001-cli-foundation/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── cli-commands.md
└── tasks.md              # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
src/
├── index.ts              # Entry point (shebang, bootstrap)
├── session.ts            # Session loop, input handling
├── commands.ts           # Command registry, /help, /exit
└── llm.ts                # Provider interface + Codex exec provider

tests/
├── commands.test.ts      # Command registry + built-in commands
├── session.test.ts       # Input parsing, empty input, command dispatch
└── llm.test.ts           # LLM client (mocked API calls)
```

**Structure Decision**: Single project layout. Four source files
aligned with functional responsibilities (Principle III). No
subdirectories within src/ — the feature is small enough that flat
structure is simpler (Principle I).
